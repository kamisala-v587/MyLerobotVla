Lerobot框架 替换openpi的VLM骨架为Qwen3
这跟手搓没啥区别

**当前进展**

+ 自定义lerobot policy 
+ my policy 实现前向, 推理

**下一步**
+ 训练 mypolicy
+ 迁移qwen3 到lerobot中,实现前向,推理

# 1. 安装 LeRobot（若尚未安装）
cd /vla/my_vla/src/lerobot && pip install -e .

# 2. 安装你的 Policy 插件
cd /path/to/lerobot_policy_my_policy && pip install -e .

# 3. 验证注册是否成功

```

```

# 4. 开始训练
cd /vla/my_vla/src/lerobot
conda activate myvla

**安装 demo policy 插件（已安装可跳过）**
pip install -e /vla/my_vla/policy/demo

**从零训练 demo_policy（无需 --policy.path）**
accelerate launch -m lerobot.scripts.lerobot_train \
  --config_path=/vla/my_vla/policy/config.json


### 6.8 验证VLM Frozen 以及AE随机化效果

cd /vla/my_vla/src/lerobot
conda activate myvla
export HF_HUB_OFFLINE=1
export TRANSFORMERS_OFFLINE=1
export TOKENIZERS_PARALLELISM=false

CUDA_VISIBLE_DEVICES=0,1

accelerate launch -m lerobot.scripts.lerobot_train \
  --config_path=/vla/my_vla/policy/config.json \
  --dataset.repo_id=/vla/.data/test \
  --policy.path=/vla/.models/lerobot-pi05_base

accelerate launch -m lerobot.scripts.lerobot_train \
  --config_path=/vla/my_vla/policy/config.json \
  --dataset.repo_id=/vla/.data/test \
  --policy.path=/vla/.models/pi05_AE_random


### 6.9 训练vla
利用robotwin 生成的数据（adjust bottle） 转换为lerobot3.0 进行微调
+ 全参微调openpi05-base
+ 冻结VLA，随机化AE，微调my_pi05模型

cd /vla/my_vla/src/lerobot
conda activate myvla
export HF_HUB_OFFLINE=1
export TRANSFORMERS_OFFLINE=1
export TOKENIZERS_PARALLELISM=false

CUDA_VISIBLE_DEVICES=0,1
accelerate launch -m lerobot.scripts.lerobot_train \
  --config_path=/vla/.models/pi05_AE_random/LerobotConfig.json \
  --dataset.repo_id=/vla/.data/adjust_bottle \
  --policy.path=/vla/.models/pi05_AE_random

accelerate launch -m lerobot.scripts.lerobot_train \
  --config_path=/vla/.models/lerobot-pi05_base/LerobotConfig.json \
  --dataset.repo_id=/vla/.data/adjust_bottle \
  --policy.path=/vla/.models/lerobot-pi05_base

**6.20 预训练启动**
cd /vla/my_tbot
conda activate tbot_sa1
export HF_HUB_OFFLINE=1
export TRANSFORMERS_OFFLINE=1
export TOKENIZERS_PARALLELISM=false

accelerate launch --multi_gpu --num_processes=2 \
  -m lerobot.scripts.lerobot_train \
  --config_path=/vla/my_tbot/.配置/pretrain_config.json


### 临时
/vla/.models/tbot_ckpts/100000/pretrained_model
python .code/serve_lerobot_policy.py   --ckpt_path /vla/.models/tbot_ckpts/100000/pretrained_model  --qwen3_vl_pretrained_path /vla/.models/Qwen3-VL-2B-Instruct   --qwen3_vl_processor_path /vla/.models/Qwen3-VL-2B-Instruct   --cosmos_tokenizer_path_or_name /vla/.models/Cosmos-Tokenizer-CI8x8   --host 0.0.0.0 --port 8000   --infer_horizon 50  --action_mode delta